apiVersion: projectcontour.io/v1
kind: HTTPProxy
metadata:
  name: go-web-server-ingress
spec:
  tcpproxy:
    services:
    - name: go-web-server-service
      port: 8080
  virtualhost:
    fqdn: ${SESSION_NAMESPACE}-app.${INGRESS_DOMAIN}
    tls:
      passthrough: true